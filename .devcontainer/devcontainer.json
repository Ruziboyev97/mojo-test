{
    "name": "Mojo Development with Pixi",
    "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
    
    "postCreateCommand": "curl -fsSL https://pixi.sh/install.sh | bash && source ~/.bashrc && pixi install mojo && echo 'eval \"$(pixi shell --help)\"' >> ~/.bashrc",
    
    "customizations": {
        "vscode": {
            "extensions": [
                "modular-mojotools.vscode-mojo"
            ]
        }
    }
}